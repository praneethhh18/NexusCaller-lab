# ═══════════════════════════════════════════════════════════════════════
#   Core — required for every session
# ═══════════════════════════════════════════════════════════════════════
pipecat-ai[silero,whisper,ollama,kokoro,piper]>=0.0.50
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9
numpy>=1.26
soundfile>=0.12
openai>=1.50
python-dotenv>=1.0
loguru>=0.7
pydantic>=2.8
setuptools<81                         # librosa 0.9.x / MeloTTS still imports pkg_resources
torchaudio==2.4.1                     # keep matched with torch 2.4.1 on this Windows venv

# ═══════════════════════════════════════════════════════════════════════
#   Optional extras — install only the ones whose models you'll test.
#   Each block is independent; comment out what you don't need.
# ═══════════════════════════════════════════════════════════════════════

# STT — beyond Whisper
# ---------------------------------------------------------------------
useful-moonshine>=0.1                   # Moonshine — tiny streaming STT
# nemo-toolkit[asr]                     # NVIDIA Parakeet (large dep, GPU)

# TTS — Coqui XTTS (voice cloning, non-commercial free)
# ---------------------------------------------------------------------
# TTS>=0.22                             # Coqui TTS package (XTTS v2)

# TTS — custom wrappers (uncomment per pipeline/tts_custom.py)
# ---------------------------------------------------------------------
# f5-tts                                # F5-TTS (non-commercial)
# styletts2                             # StyleTTS 2 (MIT)
# parler-tts                            # Parler-TTS (Apache 2.0)
# ChatTTS                               # ChatTTS (non-commercial)
# suno-bark                             # Bark (MIT)
# git+https://github.com/myshell-ai/MeloTTS.git  # MeloTTS (MIT); PyPI sdist is broken
# sherpa-onnx                           # OmniVoice via k2-fsa

# Audio perf on Intel CPUs (optional 1.5-2x speedup)
# ---------------------------------------------------------------------
# openvino>=2024.4
